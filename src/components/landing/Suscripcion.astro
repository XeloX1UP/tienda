<div class="bg-linear-to-r from-bg-300 to-bg-200 rounded-lg p-6 mt-8">
                <div class="container mx-auto grid grid-cols-1 md:grid-cols-2 gap-6 items-center">
                    <div class="space-y-2">
                        <h3 class="font-bold text-2xl">Suscríbete y recibe ofertas</h3>
                        <p class="text-text-200">Sé el primero en enterarte de lanzamientos y descuentos exclusivos.</p>
                    </div>
                    
                    <form class="grid grid-cols-[1fr,auto] gap-3" onsubmit="event.preventDefault()">
                        <input 
                            type="email" 
                            placeholder="Tu correo electrónico" 
                            aria-label="Correo electrónico" 
                            required 
                            class="w-full border border-accent-200 p-2 rounded-md focus:ring-2 focus:ring-accent-100 focus:border-transparent outline-none"
                        />
                        <button 
                            type="submit" 
                            class="bg-accent-100 text-white py-2 px-6 rounded-md hover:bg-accent-200 transition-colors"
                        >
                            Suscribirse
                        </button>
                    </form>
                </div>
            </div>